import { useState } from 'react';

function SearchBar() {
  return (
    <div>
      Search
      <br />
      <input type="text" placeholder="Search.." />
    </div>
  );
}

export default SearchBar;
